# ADR-012：仿真并发上限可扩展

> 状态：已接受

当前实现并发仿真上限为 `1`，但单仿真是当前运行策略，不是永久领域约束。外部接口和目标数据模型必须通过 `simulation_id` 和运行引用集合支持扩展。

提高并发上限时，必须把全局状态迁移为按 `simulation_id` 索引的注册表，并隔离 Agent 状态、容器槽位、事件队列、通信矩阵、运行配置、日志、PCAP、manifest 和网络 profile。不得把当前单例状态固化成永久外部接口。
