# ADR-015：剧本管理与仿真运行数据模型分域

> 状态：已接受

剧本管理模型只包含静态剧本内容和上传、下载、删除、详情、列表过程数据。`SimulationRun`、`SimulationRuntimeConfig`、`SimulationEvent`、运行引用和运行状态统一归属于仿真编排领域。

剧本删除通过稳定的占用判断接口查询活动使用，不复制或泄漏仿真注册表结构。不得让剧本模型持有仿真运行对象，也不得在两个领域复制同一运行模型形成双重权威。
