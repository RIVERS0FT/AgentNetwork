# ADR-017：剧本固定以目录形式存储

> 状态：已接受

平台内部剧本资源固定以目录形式存储。一个 `scene_key` 对应一个剧本目录，`SceneResource.storage_locator` 定位该目录；下载归档只是传输输出，不改变内部存储合同。

不得恢复只含 `folder` 一个值的格式枚举，也不得在列表、详情或 API 中暴露无意义的格式字段。支持新组织形式时必须重新设计完整生命周期合同。
