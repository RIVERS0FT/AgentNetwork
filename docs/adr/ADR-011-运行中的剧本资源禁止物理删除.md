# ADR-011：运行中的剧本资源禁止物理删除

> 状态：已接受

## 决定

删除剧本前必须查询运行占用状态。仍被运行中仿真依赖的剧本不得物理删除。

## 原因

剧本基本结构加载到内存不代表全部 Skill、Tool 和运行资源已脱离持久化目录。删除正在使用的资源可能使后续读取或执行失败。

## 禁止回退

- 不得以“结构已加载到内存”为由移除占用检查；
- 不得承诺删除运行中原始剧本后仿真必然正常；
- 没有完整运行快照时不得物理删除被占用剧本。

未来若启动时创建完整不可变快照且运行过程只访问该快照，可由新 ADR 替代本决定。
